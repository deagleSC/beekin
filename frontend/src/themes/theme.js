import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
        main: "#fffff",
        },
        secondary: {
        main:  "#888888"
        },
        button: {
        main: "#1071FF" 
        }
    },
    shadows: ["none"],
    
});